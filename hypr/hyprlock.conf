source = /home/jagan/.cache/wal/colors-hyprland.conf

background {
    monitor =
    path = /home/jagan/.config/hypr/wallpaper/wallpaper_84.png
    color = $color0
    blur_size = 5
    blur_passes = 1 # 0 disables blurring
    contrast = 1.3000 # Vibrant!!!
    brightness = 0.8000
    vibrancy = 0.2100
    noise = 0
    vibrancy_darkness = 0.0
}

# GENERAL
general {
    no_fade_in = false
    grace = 0
    disable_loading_bar = false
}

input-field {
    monitor =
    size = 250, 50
    outline_thickness = 3
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 1.00 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = $color4
    inner_color = $color0
    font_color = $color3
    fade_on_empty = false
    placeholder_text = <i><span foreground="##ffffff99">Hi, $USER mavayya</span></i>
    hide_input = false
    #fail_color = rgba(204, 34, 34, 0) # if authentication failed, changes outer_color and fail message color
    #fail_text = $FAIL <b>($ATTEMPTS)</b> # can be set to empty
    #fail_transition = 300 # transition time in ms between normal outer_color and fail_color
    position = 0, 60
    halign = center
    valign = bottom
    capslock_color = -1
    numlock_color = -1
    bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
    invert_numlock = false # change color if numlock is off
    swap_font_color = false # see below

    #monitor =
    #size = 300, 30
    #outline_thickness = 0
    #dots_size = 0.25 # Scale of input-field height, 0.2 - 0.8
    #dots_spacing = 0.55 # Scale of dots' absolute size, 0.0 - 1.0
    #dots_center = true
    #dots_rounding = -1
    #outer_color = rgba(242, 243, 244, 0)
    #inner_color = rgba(242, 243, 244, 0)
    #font_color = rgba(242, 243, 244, 0.75)
    #fade_on_empty = false
    #placeholder_text = # Text rendered in the input box when it's empty.
    #hide_input = false
    #check_color = rgba(204, 136, 34, 0)
    #fail_color = rgba(204, 34, 34, 0) # if authentication failed, changes outer_color and fail message color
    #fail_text = $FAIL <b>($ATTEMPTS)</b> # can be set to empty
    #fail_transition = 300 # transition time in ms between normal outer_color and fail_color
    #capslock_color = -1
    #numlock_color = -1
    #bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
    #invert_numlock = false # change color if numlock is off
    #swap_font_color = false # see below
    #position = 0, -600
    #halign = center
    #valign = center
}



# Current time
#label {
    monitor =
    #text = cmd[update:1000] echo "$(date +"%H")"
    color = $color6
    font_size = 128
    font_family = IBM Plex Sans Medium 10
    position = 0, 20
    halign = center
    valign = center
}
label {
    monitor =
    #text = cmd[update:1000] echo "$(date +"%M")"
    text = cmd[update:1000] echo "$(date +"%k:%M")"
    color = $color6
    font_size = 128
    font_family = IBM Plex Sans Medium 10
    position = 0, -100
    halign = center
    valign = center
}
label {
    monitor =
    #text = cmd[update:1000] echo "$(date +"%d %b")"
    text = cmd[update:1000] echo "$(date +"%A, %B %d")"
    color = $color7
    font_size = 16
    font_family = SF Pro Rounded Bold
    #font_family = IBM Plex Sans Bold 10
    position = 0, -250
    #position = 0, 400
    halign = center
    valign = center
}

#label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%A")"
    color = $color3
    font_size = 16
    font_family = IBM Plex Sans Medium 10
    position = 40, -250
    halign = center
    valign = center
}

# Location
label {
    monitor =
    text = cmd[update:86400000] echo "$(bash /home/jagan/.config/hypr/scripts/city)"
    color = $color11
    font_size = 30
    #font_family = SF Pro Rounded Bold
    font_family = IBM Plex Sans Bold 10
    #font_family = IBM Plex Sans Bold
    position = -10, 109
    halign = right
    valign = bottom
}

# Feels like temperature (weather.sh)
label {
    monitor =
    text = cmd[update:3600000] echo "Feels like $(bash /home/jagan/.config/hypr/scripts/weather)"
    color = $color9
    font_size = 14
    font_family = SF Pro Rounded
    position = -10, 10
    halign = right
    valign = bottom
}

# Current temperature and weather description (sky.sh)
label {
    monitor =
    text = cmd[update:3600000] echo "Temp: $(bash /home/jagan/.config/hypr/scripts/current_temp)°C - $(bash /home/jagan/.config/hypr/scripts/sky)"
    color = $color9
    font_size = 14
    font_family = SF Pro Rounded
    position = -10, 35
    halign = right
    valign = bottom
}


# Humidity
label {
    monitor =
    text = cmd[update:3600000] echo "Humidity: $(bash /home/jagan/.config/hypr/scripts/humidity)"
    color = $color9
    font_size = 14
    font_family = SF Pro Rounded
    position = -10, 60
    halign = right
    valign = bottom
}

# Wind Speed
label {
    monitor =
    text = cmd[update:3600000] echo "Wind: $(bash /home/jagan/.config/hypr/scripts/wind_speed) mp/h"
    color = $color9
    font_size = 14
    font_family = SF Pro Rounded
    position = -10, 85
    halign = right
    valign = bottom
}

# Current song
label {
    monitor =
    text = cmd[update:1000] echo "$(bash /home/jagan/.config/hypr/scripts/songdetail)"
    color = $color33
    font_size = 14
    #font_family = SF Pro Rounded Bold
    font_family = IBM Plex Sans Bold 10
    position = 10, 10
    halign = left
    valign = bottom
}

# Network Status
label {
  monitor =
  text = cmd[update:1000] echo "$(~/.config/hypr/scripts/network-status)"
  color = $color33
  font_size = 15
  font_family = FiraCode Nerd Font
  position = -120, -10
  halign = right
  valign = top
}

# Battery Status
label {
  monitor =
  text = cmd[update:1000] echo "$(~/.config/hypr/scripts/battery-status)"
  color = $color33
  font_size = 14
  font_family = SF Pro Text
  font_family = IBM Plex Sans Bold 10
  position = -70, -10
  halign = right
  valign = top
}

# keyboard layout
label {
  monitor =
  text = cmd[update:1000] echo "$(~/.config/hypr/scripts/layout-status)"
  color = $color33
  font_size = 15
  #font_family = SF Pro Text
  font_family = IBM Plex Sans Bold 10
  position = -20, -10
  halign = right
  valign = top
}


image {
    monitor =
    path = $HOME/.config/rofi/images/lock_screen.png
    #path = $HOME/.config/rofi/images/user.png
    border_color = 0xffdddddd
    border_size = 0
    size = 150
    rounding = 500
    rotate = 0
    reload_time = -1
    reload_cmd = 
    position = 0, -400
    halign = center
    valign = center
}

#label {
    monitor =
    text = Hii Jagan Mavayya
    color = $color9
    font_size = 20
    font_family = IBM Plex Sans Bold 10
    position = 0, -370
    halign = center
    valign = center
}
